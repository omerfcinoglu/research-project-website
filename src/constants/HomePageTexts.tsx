import { enTexts } from "@/i18n/en";

interface HomePageTexts {
  project_title: string;
  project_coordinator: string;
  project_school: string;
}

const HomePage_Texts: HomePageTexts = {
  project_title: enTexts.home.project_title,
  project_coordinator: enTexts.home.project_coordinator,
  project_school: enTexts.home.project_school,
};

export default HomePage_Texts;
